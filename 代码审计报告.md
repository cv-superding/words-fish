# words-fish 代码库全面审计报告

> 审计范围：`src/main/*`（主进程）、`src/preload/*`、`src/renderer/*`（含 popup / bubble / knowledge / settings）。
> 说明：以下“行号”基于本次读取时的实际源码。严重程度优先级：**高 → 中 → 低**。

---

## 🔴 高严重度（会导致核心功能在多数环境下静默失效）

### 1. 全屏检测误判，导致定时推送对绝大多数办公用户彻底失效
- **位置**：`src/main/scheduler.js:39-59`（`isFullscreenPlaying`）、`:87-98`（`tick`）
- **类型**：逻辑错误 / 误报（false positive）
- **触发条件**：`push.pauseWhenFullscreen`（默认 `true`，见 `config.js:28`）开启时，只要本机运行了 `PowerPoint`、或后台常驻的 `Teams` / `WeMeet`(`wemeetapp`) / `WebEx`(`webexmta`) 等进程之一，就被判定为“正在全屏”而跳过本次推送。
- **后果**：这些会议/聊天软件在办公电脑上几乎**常驻后台**，并非真正全屏。于是 `tick()` 每次都 `shouldFire` 通过、却在全屏分支里 `planNext` 跳过 → 定时气泡推送实际上**几乎永远不弹出**。这是“摸鱼背单词”的核心功能，却对绝大多数用户静默失效。
- **建议**：用“前台窗口是否真正全屏（窗口尺寸==屏幕尺寸）”判定，而非“进程是否在运行”；至少应立即从热名单中移除 Teams/WeMeet/WebEx/PowerPoint（它们不表示全屏）。

---

## 🟠 中严重度

### 2. `config:update` 重复触发 `onConfigChanged`，造成双重广播 / 双重重渲染
- **位置**：`src/main/ipc.js:37-42`（`config:update` 处理器显式调用 `onConfigChanged`）与 `:221`（`config.onChange` 监听器在 `config.update()` 内部也调用 `onConfigChanged`）
- **类型**：冗余调用 / 逻辑缺陷（同一事件被处理两次）
- **触发条件**：任何来自设置面板或悬浮窗的 `config:update`（几乎每一次用户改设置）。
- **后果**：`onConfigChanged`（`:287-302`）对每次变更执行 `scheduler.reconfigure()`、`hotkeys.applyAll()`、`tray.refresh()` 以及 `wins.broadcast('config:changed', …)`——**全部执行两遍**。悬浮窗 / 知识页会因此被 `api.current()` 重载两次，出现不必要的重渲染与闪烁。
- **建议**：删掉 `ipc.js:40` 的显式 `onConfigChanged` 调用，统一由 `config.onChange` 监听器触发即可。

### 3. 知识会话历史无上限，导致 Token 成本膨胀并最终撑爆上下文窗口
- **位置**：`src/main/knowledge.js:239-261`（`ask` 每次 `session.history.push`）、`:215-230`（`buildMessages` 把**整段** `session.history` 发给 LLM）
- **类型**：内存/磁盘增长 + 边界条件未处理
- **触发条件**：任一知识领域会话较长、或多轮对话后。
- **后果**：
  - 每次提问都把全部历史塞进 `messages`，Token 消耗随对话线性增长，且到达模型上下文上限后会直接返回 API 错误；
  - `knowledge.json`（`saveNow` `:143-157`）随会话无限膨胀，磁盘与内存同步增长。
- **建议**：对发给 LLM 的历史做窗口裁剪（如保留最近 N 轮 + 摘要），并对持久化历史设定上限/滚动归档。

### 4. `knowledge.ask` 在 LLM 失败时留下“悬空用户消息”
- **位置**：`src/main/knowledge.js:244`（先 `session.history.push` 用户消息）→ `:245-250`（`llm.chatCompletions` 可能抛错）；`src/main/ipc.js:108-121`（`knowledge:ask` 处理器未 try/catch）
- **类型**：数据一致性缺陷 / 未捕获异常
- **触发条件**：任何 LLM 超时、网络中断、4xx/5xx。
- **后果**：用户提问已写入历史，但助手回复永远缺失；异常冒泡到 IPC `invoke` 被 reject（渲染端 `send()` 虽能兜底显示错误文本），下次打开该会话时会看到一条没有回复的用户提问。
- **建议**：在 `ask` 中将“写历史”放到 LLM 成功返回之后，或失败时回滚未成对的用户消息；`knowledge:ask` 处理器应捕获异常并返回结构化错误。

### 5. 气泡（bubble）的“跳过/记住了”按钮不推进、且把气泡冻住
- **位置**：`src/renderer/bubble/bubble.js:46-55`
- **类型**：逻辑缺陷（推进结果未被渲染）
- **触发条件**：在托盘气泡上点击“⏭ 跳过”或“✓ 记住了”。
- **后果**：按钮依次调用 `api.markUnknown()/markKnown()` → `api.hold()`（**清掉自动关闭定时器**）→ `api.next()`。`api.next()` 只是 `invoke('study:word','next')` 返回新词，但气泡**只**监听 `word:update` 广播来重绘（`:59`），而 `next()` 不会广播。结果：气泡继续显示**旧单词**，且因 `hold()` 清了定时器而**永远不消失**，必须手动关闭。
- **建议**：与悬浮窗按钮一致——用返回的新 payload 直接 `render()`，或在 `next()` 后向 bubble 广播 `word:update`；`hold()` 不应在“换词”路径上调用。

### 6. 托盘单击 + 双击都绑定 `togglePopup`，双击变成“无操作 + 闪烁”
- **位置**：`src/main/tray.js:135-137`
- **类型**：事件冲突 / 逻辑缺陷
- **触发条件**：在 Windows 上对托盘图标双击（会先触发 `click` 再触发 `double-click`）。
- **后果**：一次双击 = 两次 `togglePopup` = 状态回到原点（并伴随一次显隐闪烁）。左键单击切换悬浮窗本就是预期交互，多余的 `double-click` 绑定纯属干扰。
- **建议**：二选一，保留 `click`（切换）并删除 `double-click` 绑定。

### 7. 悬浮窗“手动隐藏释义”会被任何配置广播重置
- **位置**：`src/renderer/popup/popup.js:383-390`（监听 `config:changed` → `api.current().then(loadWord)`）；`loadWord` `:367-371` 重置 `state.revealed = !payload.view.meaningHidden`
- **类型**：状态覆盖 / UX 缺陷
- **触发条件**：用户在悬浮窗点了“隐藏释义”，随后发生任何配置变更（改主题/字号/间隔等都会广播 `config:changed`）。
- **后果**：广播触发 `loadWord` 重新计算 `revealed`，把用户手动隐藏的释义又显示出来，自测模式被无声打断。
- **建议**：`onConfig` 重载时仅刷新与显示相关的字段（主题/尺寸等），不要调用会重置 `revealed` 的 `loadWord`；或把 `revealed` 与“配置驱动的 meaningHidden”分离。

---

## 🟡 低严重度 / 代码异味（建议择机修正）

### 8. `app:setAutoLaunch` 忽略入参、读取陈旧配置
- **位置**：`src/main/ipc.js:208-211`（`refreshAutostart()` 读 `config.general.autoLaunch`，而非传入的 `enabled`）；`src/renderer/settings/settings.js:94-97`（先 `setAutoLaunch` 再 `onCfgInputChange`）
- **类型**：参数未使用 / 时序缺陷
- **触发条件**：在设置里切换“开机自启”。
- **后果**：该调用在配置尚未写入前执行、且完全不用传入的 `enabled`，属于无效/错误的中间调用。功能上由随后 `onCfgInputChange` 触发的 `onConfigChanged` 自动启动分支（`:295-297`）兜底修正，故**实际不失效**，但入参与执行顺序明显错误。
- **建议**：先 `config.update({general:{autoLaunch}})`，再由 `onConfigChanged` 统一 `refreshAutostart`；或让该处理器直接 `config.update` + `refreshAutostart(enabled)`。

### 9. DEFAULTS 缺少 `popup.size`，与持久化形状不一致
- **位置**：`src/main/config.js:42-56`（popup 只有 `width:380`，无 `size`）；`src/main/windows.js:43-45`（`config.get('popup.size',{})` 再回退 `popup.width`/240）
- **类型**：配置不一致
- **后果**：首次创建悬浮窗可用，但默认值与运行时实际持久化的 `popup.size` / `popup.knowledgeSize` 结构不一致，后续若依赖默认值会踩坑。低风险。

### 10. `stats.totals` 永远不累计 `exposed`
- **位置**：`src/main/records.js:114-120`（`bump` 仅当 `totals[name]` 已存在才累加；`totals` 初始化为 `{learned,reviewed,marked}`，无 `exposed`）
- **类型**：聚合遗漏
- **后果**：按日 `exposed` 正常统计，但累计聚合 `totals.exposed` 始终缺失（`statsSnapshot` 返回的对象少一个字段）。`today` 维度的曝光不受影响。

### 11. `getBook()` 在“读取”路径里写配置（副作用）
- **位置**：`src/main/wordflow.js:16-24`；被 `buildPayload`/`next`/`statsSnapshot` 反复调用
- **类型**：读写不分 / 潜在副作用
- **后果**：`getBook` 在词库 id 失效时会 `config.update({study:{activeBookId}})`，在导航过程中触发一次配置广播（进而触发第 2 条的双重广播）。正常情况 id 有效时不写，无影响，但属于不良设计；若将来 `onConfig` 重载路径与 `getBook` 构成回环，会放大为潜在死循环（当前因“有效 id 不写”未被触发，记为隐患）。

### 12. 代理 CONNECT 路径缺少显式超时处理
- **位置**：`src/main/http.js:111-136`（外层 `conn` 设了 `timeout: timeoutMs` 却无 `'timeout'` 监听器，仅挂了 `'error'`；内层 TLS 请求有 `attachTimeout`）
- **类型**：资源悬挂 / 潜在内存泄漏
- **触发条件**：配置 HTTP 代理且目标为 HTTPS，且代理“接住 TCP 但不回 CONNECT 响应”。
- **后果**：`conn` 既不 emit `error` 也不进入 `connect` 时，Promise 永不 settle，socket 与闭包泄漏，LLM 请求卡死。
- **建议**：为 `conn` 也加 `'timeout'` 监听器并 `destroy()`。

### 13. 流式模式下错误详情被丢弃
- **位置**：`src/main/http.js:90`（流式 `resp.body` 置 `null`）；`src/main/llm.js:104-110`（出错时 `JSON.parse(resp.body||'{}')`）
- **类型**：错误信息降级
- **后果**：流式请求返回 4xx/5xx 时，真实 API 错误体因 `body=null` 无法解析，只显示 “HTTP <code>”，排查困难。

### 14. 知识会话永不删除，数量无上限累积
- **位置**：`src/main/knowledge.js:183-195`（`getOrCreate` 只增不删；无删除会话接口）
- **类型**：数据增长
- **后果**：跨领域/长期使用的会话在 `knowledge.json` 中持续累积（配合第 3 条进一步放大磁盘增长）。建议提供“删除会话”入口并限制保留数量。

### 15. 托盘菜单 `accelerator` 是误导性的
- **位置**：`src/main/tray.js:52,100`
- **类型**：UI 误导（低）
- **后果**：上下文菜单项的 `accelerator` 仅在菜单**打开时**有效，并非全局热键；真正的全局快捷键由 `hotkeys.js` 注册。菜单上标 “Shift+X” 容易让用户误以为托盘菜单本身响应快捷键。

### 16. `records.pick` 用引用相等判定来源（脆弱）
- **位置**：`src/main/records.js:288`（`chosen.list === fresh`）
- **类型**：潜在脆弱性（低）
- **后果**：依赖 pool 中 `list` 与 `fresh/marked/due` 数组是同一引用才正确；一旦未来重构 pool 构造方式（如复制数组），来源判定会静默错误。建议改用显式 `source` 标记。

---

## 已确认「无问题」或设计合理的点（避免误报）
- **XSS**：`knowledge.js` 渲染器的 `mdToHtml`/`inline` 先 `escapeHtml` 再格式化；`<a>` 仅放行 `https?`；各渲染层对词库/配置文本均做了 `esc()`。未发现注入点。
- **CSP**：各 `index.html` 均为 `script-src 'self'` + `style-src 'self' 'unsafe-inline'`，未暴露 `unsafe-eval`，无内联脚本。
- **单实例锁 / 崩溃日志 / before-quit 落地**：`main.js` 处理得当。
- **热键注册失败兜底**：`hotkeys.js` 单键失败不影响其他键。
- **配置文件深合并 / 持久化**：`config.js` 逻辑正确。
- **词库导入解析**（JSON/JSONL/CSV/TSV/TXT）覆盖较全，去重与 BOM 处理到位。

---

## 修复优先级建议
1. **立即修**：#1（全屏误判，核心功能失效）、#4（失败留悬空消息）、#5（气泡按钮冻住）。
2. **尽快修**：#2（双重广播）、#3（历史无上限）、#6（托盘双击冲突）、#7（释义重置）。
3. **择机清理**：#8–#16（低严重度代码质量项）。
